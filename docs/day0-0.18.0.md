# 0.18.0 day-0 pack — one table, run in this order on the day the GitHub tag lands

Trigger: `gh release view v0.18.0 --repo google-ai-edge/LiteRT-LM` returns a release (not the PyPI wheel: 0.17.0 and 0.17.1 reached PyPI 4 days and 22 h before their tags). Drafted 2026-09-17 against `main` at 4e3d7d18f (`version.bzl` = 0.18.0, `C_API_VERSION` = 0.2.0); nothing below has been run against 0.18.0 yet. Re-read 2026-09-21 against `main` at 5a34c6c40 (42 commits later): no v0.18.0 release, `version.bzl` unchanged, `LITERT_REF` still in `WORKSPACE`, PyPI nightly = `0.18.0.dev20260919`. Rows 1, 2 and 4 changed and row 10 is new. Each row is a verdict with its expected value and the command that measures it. Anything posted upstream is drafted first and goes out on GO.

Before step 1: `GEN_LITERTLM_TAG=v0.18.0`, `GEN_PYPI_LITERT_LM=0.18.0`, `GEN_MAVEN_LITERTLM_ANDROID=0.18.0`, `GEN_NPM_LITERTLM_CORE=0.18.0` in `pins.env`, then `rm -rf cache/ghlm` — `channels_common.download()` reuses a cached file whose size equals the asset size, so a same-size zip under a new tag would be read from the old bytes.

| # | Verdict | Expected (PASS =) | Command | If not PASS |
|---|---|---|---|---|
| 1 | **Tag facts** (first 10 min): asset list with timestamps, tag commit, `Package.swift` pins at the tag, header set in the mac zip | Assets = `CLiteRTLM.xcframework.zip`, `CLiteRTLM_mac.xcframework.zip`, `litert_lm_main.macos_arm64`, `litert_lm_c_api-0.2.0.zip`, `CLiteRTLM.spdx.json`, `THIRD_PARTY_NOTICES.txt` (the v0.16.0 shape). Pins point at `download/v0.18.0/`. Mac zip `Headers/` = the seven public headers `c/BUILD` lists on `main`: `api_export.h`, `conversation.h`, `embedding_engine.h`, `engine.h`, `error_reporter.h`, `experimental.h`, `model_info.h`. `capabilities.h` (in the v0.17.1 zip) is gone: c1cf63794 renamed it to `model_info.h`. `api_export.h` is new (3468961f1, 09-17); `engine.h`, `experimental.h` and `error_reporter.h` include it, so a zip without it fails at the first include. `swift/` on `main` calls `model_info.h` and `error_reporter.h` | `gh release view v0.18.0 --repo google-ai-edge/LiteRT-LM --json assets,publishedAt --jq '.'` · `gh api 'repos/google-ai-edge/LiteRT-LM/git/ref/tags/v0.18.0'` · `gh api 'repos/google-ai-edge/LiteRT-LM/contents/Package.swift?ref=v0.18.0' --jq .content \| base64 -d \| grep -n -A2 'url:'` · `unzip -l CLiteRTLM_mac.xcframework.zip \| grep Headers/` | Each missing item is a row of [release-asset-checklist.md](release-asset-checklist.md); the tag-day table goes on the checklist issue (if open) or on the row's own issue |
| 2 | **Gate 1b**: the GitHub-asset, SwiftPM and README rows on the tag, then npm / Maven (phone attached), then the table and its diff against v0.17.0 | 0 new FAIL. Expected flips: `swiftpm.build` PASS only if row 1's pins are on v0.18.0; `quickstart.gemma4-*` FAIL→PASS (README fix 49cf34756 is on `main` since 09-04; PASS iff the cut is after it); `ghlm.c-api-zip` cell says *hosted on v0.18.0* (the runner falls back to older tags — a PASS on a v0.16.0 zip is not a 0.18.0 result); `ghlm.release-license-docs` FAIL→PASS if SBOM ships. The SwiftPM and Maven generate legs pass on exit 0 + non-empty output, and a dropped prompt satisfies both (row 10): read the OUTPUT line for 42 (the prompt is "What is 17 + 25?") before recording PASS. The canary `gemma3-270m-it-q8.litertlm` carries no jinja template (structured `prompt_templates` only, header read 09-21), so the runtime's default path renders it | `./run_channels.sh --only gh_assets,swiftpm,quickstart` → `./run_channels.sh --only npm,maven` → `python3 make_table.py --results results_$(date +%Y%m%d)` → `python3 diff_tables.py` against `results/v0.17.0-generation.md` | Every FAIL→PASS = a fix-confirmed comment seed (#3296 item 2, #3418, #3194, #2407 only with a fresh `--cache-path`); every PASS→FAIL = a regression comment the same day; a FAIL with no issue = new finding, draft only |
| 3 | **TTS shipped or not** — does any 0.18.0 artifact expose `TtsEngine` (Kokoro / Qwen3 TTS, on `main` since 7b0415bcf 08-31; Kokoro vocoder on GPU 7a01f227b 09-15)? | Shipped = any of: xcframework binary strings `TtsEngine\|Kokoro\|Qwen3Tts` > 0 against ASR control 0 and EmbeddingEngine control > 0; a `tts`/`audio` header in `Headers/`; AAR `classes.jar` has `*Tts*` classes; CLI lists a tts subcommand. The PyPI wheel is expected at 0 regardless (it never links `omni/`, RESULTS_TTS_WHEEL_0170) | `tts_assets_check.sh <xcf_dir> <aar>` (litertlm-convert `watch/on_release/results_20260909/`) · `tts_wheel_check.sh 0.18.0 0.17.1 python3.12` (`results_20260908/`) · `litert-lm --help \| grep -i tts` · `ls Headers/ \| grep -iE 'tts\|audio'` | Shipped → run the Qwen3-TTS day-0 folder (`litertlm-convert/out/qwen3tts-day0/folder`, local, unpublished) through the shipped surface; PASS = ASR round-trip of the output WAV (faster-whisper) matches the input text; publication (HF files, card, collection) is a separate GO. Not shipped → the folder stays local; add the 0.18.0 line to `qwen3tts_work/DAY0_FOLDER_FINDINGS.md` |
| 4 | **C API resumed?** (#3569, closed 09-15 with "0.18.0 very soon") | `litert_lm_c_api-0.2.0.zip` present with the v0.16.0 platform set (seven library files for six platforms) and the seven headers of row 1; renamed functions (whhone: "renaming some new functions") listed by a header diff against the v0.16.0 zip. 3468961f1 ("Define LiteRtLmStatusCode enum and return canonical status codes") landed after the draft, so the diff reads return types too, not names only | `unzip -l litert_lm_c_api-0.2.0.zip` · `diff <(grep -ho 'litert_lm_[a-z_]*' v0.16.0/include/*.h \| sort -u) <(grep -ho 'litert_lm_[a-z_]*' v0.18.0/include/*.h \| sort -u)` · `nm -gU lib/macos_arm64/liblitert-lm.dylib \| grep -c litert_lm_` · `grep -n LiteRtLmStatusCode v0.18.0/include/*.h` | Present → the rename list is what our Swift wrapper and bindings adapt to (no comment needed; the zip is the answer). Absent → record only (paused by their decision; a comment adds nothing) |
| 5 | **SwiftPM re-verification** (#3296): `exact: "0.18.0"` and `main` both build | Gate row `swiftpm.build` PASS at `exact:` (covered by step 2) **and** a `main` checkout builds against the v0.18.0 zips (item 2 of the 09-09 comment: `model_info.h` symbols) | `swift build` in a fresh clone of `main` (Xcode 27.0 / Swift 6.4 today; the v0.17.0 rows were measured on Xcode 27 beta 5) | Both PASS → close-out comment on #3296 (fix confirmed, both items). `exact:` FAIL → comment the same day with the runner's missing-symbol list (the checklist row 1 case). `main` FAIL only → one line on #3296 naming the header |
| 6 | **#3444 re-verification** (closed 09-15 "completed"; 91a926c16 clamps `max_num_tokens` against static capacity, dc911c3db errors at signature selection) | PyPI path: the 09-06 probe result unchanged (five 785-token turns, `token_count` 3926, turn 6 fails at the real 4096 wall with `remaining capacity`) — no regression. xcframework/Metal path: with `maxNumTokens: 4096` on gemma-4-E2B, no mid-conversation `Chosen prefill work group size exceeds available state entries`; either the full budget is served or init reports the ceiling | `python3 probe_3444_ceiling.py` in a venv with `litert-lm==0.18.0` (public: hf-to-litertlm `tools/runtime_probes/3444_context_ceiling/`) · the multi-turn Swift harness from the issue on `CLiteRTLM_mac` v0.18.0 (harness location not confirmed this session — find it before the day) | Fixed on Metal → fix-confirmed comment on #3444. Wall still at ~2048 → comment with the turn number and message, same shape as the original |
| 7 | **#3577 re-verification** (open; nireekshak reproduced 09-16, CPU and macOS GPU accurate, Adreno under investigation) | Same bundle (`dynamic_wi4c_hr_afp32`, 1,306,168,768 B, sha256 `6561646e…`) on `litertlm-android:0.18.0`, Galaxy S26, greedy, 8 questions: GPU answers match CPU on ≥ 7/8 = fixed; 1/8 as before = unchanged. No fix was promised for 0.18.0, so unchanged is the expected value | The instrumentation APK from the issue against the 0.18.0 AAR (harness location not confirmed this session — find it before the day); app data cleared per run, one Engine per backend | Changed either way → one comment on #3577 with the 8-question table. Unchanged → nothing (they are investigating) |
| 8 | **LFM2.5 regate**: do the shipped hybrid (ShortConv) bundles still run, and does GPU now start? | Mac: every `models.tsv` row runs; q8 scores equal the 08-11 lines (lfm25-12b-int4-shipped 7/8 cpu, lfm25-12b-int4-gpu 7/8 gpu, lfm25-26b-int4 8/8 cpu); GPU decode within ±2 % of the 2026-08-11 baselines in `models.tsv`. iPhone 17 Pro Metal gate (NEXT-SESSION-lfm25-gpu-regate.md §1, v0.18.0 xcframework checksum): GO = engine PASS + 8Q GPU ≥ CPU + decode or TTFT above CPU (52.6 tok/s / 442 ms) | `RUN_VENV=~/venvs/lt0180run watch/on_release/runtime_regate.sh --quick` then `--no-gpu` (serial, quiet Mac) · iPhone per the note (USB transfer, stale-pcm cleanup) · first: `gh api 'repos/google-ai-edge/LiteRT-LM/contents/WORKSPACE?ref=v0.18.0' \| … LITERT_REF` and whether LiteRT's `short_conv_step` fusion (#9858) is before that pin | GPU GO → the re-ship path in the note §2 (Instruct / JP / Thinking; user GO per file). FAIL → PARKED_WALLS row 21 gets the 0.18.0 signature diff; #3129 follow-up drafted |
| 9 | **Qwen3.5 regate**: gated-delta (GDN) bundles still run on CPU; record GPU | `qwen35-08b-int8` 8/8 cpu (same run as step 8). GPU on the shipped Qwen3.5-0.8B int8: record delegation count and the 8 answers — no expected value (GDN on GPU is a converter-side wave, litert-torch #1233/#1234, not a runtime switch). MTP drafter e2e on 0.18.0: acceptance equal to the recorded runs in `mtp_work/` | (step 8's run) · `litert-lm run --backend=gpu … Qwen3.5-0.8B_int8.litertlm` · `mtp_work/` e2e script with the 0.18.0 venv | Any CPU regression → the same-day regression comment. GPU numbers → the dashboard row kit (separate candidate), not a comment |
| 10 | **Chat template standard** (`models/README.md`: `content` is a list of typed parts; own #3688, whhone 09-20 "the contents will always be a list of object with a type") | The tag carries `models/README.md` and `models/chat_template_input_schema.json` with `content` as a list of parts, and 6c6b4582 (content normalized to parts) is in it. On `litert-lm==0.18.0`, one bundle per template class of the own-bundle audit (2026-09-21, `~/code/codex-conversions/2026-09-21/template-parts-audit/INVENTORY.md`, folded into litertlm-convert after GO): a dual-form or parts-only template answers the question asked; a string-only template drops the prompt (fluent answer to another question, exit 0) or raises — the class the audit predicted. Prompt = "What is the capital of France?"; the verdict is read from the text, never from the exit code | `gh api 'repos/google-ai-edge/LiteRT-LM/contents/models/README.md?ref=v0.18.0' --jq .sha` · `gh api 'repos/google-ai-edge/LiteRT-LM/compare/6c6b4582...v0.18.0' --jq .status` (`ahead` = included; `diverged` = release branch, grep `runtime/` for the normalization instead) · the audit's `scripts/round3_download.py` + `scripts/round3_run_pair.py <bundle>` with `.venv-nightly` re-created as `litert-lm==0.18.0` | Prediction holds → the audit's re-ship list goes ahead, one GO per file. A string-only bundle answers on-prompt → the runtime kept a string path: re-read the data processor before re-shipping anything. A dual-form bundle fails → same-day comment on #3688 with the rendered prompt |

## Outputs of the day

1. `results/v0.18.0-generation.md` (table + diff at the top) and `pins.env` moved — committed.
2. Comment seeds, each its own GO: #3296 (step 5), #3444 (step 6), #3577 only if changed (step 7), the checklist issue with the tag-day table (step 1), #3129 if LFM2.5 GPU moved (step 8).
3. TTS decision (step 3) and the LFM2.5 GPU verdict (step 8) written back to `DAY0_FOLDER_FINDINGS.md` and `PARKED_WALLS.md` row 21.
4. Template verdicts (step 10) written into the audit's list; each re-ship is its own GO.

Rough wall-clock: steps 1–2 about 45 min (SwiftPM mirror and README downloads dominate), 3–5 about 30 min, 6 about 30 min, 7 about 1 h (device), 8–9 about 2 h (serial Mac runs plus the iPhone gate), 10 about 30 min (one download per class, CPU). One session.

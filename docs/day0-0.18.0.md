# 0.18.0 day-0 pack — one table, run in this order on the day the GitHub tag lands

Trigger: `gh release view v0.18.0 --repo google-ai-edge/LiteRT-LM` returns a release (not the PyPI wheel: 0.17.0 and 0.17.1 reached PyPI 4 days and 22 h before their tags). Drafted 2026-09-17 against `main` at 4e3d7d18f (`version.bzl` = 0.18.0, `C_API_VERSION` = 0.2.0); nothing below has been run against 0.18.0 yet. Re-read 2026-09-21 against `main` at 5a34c6c40 (42 commits later): no v0.18.0 release, `version.bzl` unchanged, `LITERT_REF` still in `WORKSPACE`, PyPI nightly = `0.18.0.dev20260919`. Rows 1, 2 and 4 changed and row 10 is new. Re-read 2026-09-26 against `main` at e2952685d[^main] (45 commits after 5a34c6c40): still no v0.18.0 release or tag, `version.bzl` unchanged, PyPI nightly = `0.18.0.dev20260923`[^nightly], `LITERT_REF` = 26895c9f[^litertref]; LiteRT bumped QAIRT to 2.50 on 09-25, as it bumped it to 2.49 on 09-04 before v0.17.0[^qairt]. Changed: one new C function (row 4); TTS/ASR became a C++-only `omni/` API that no binding or CLI on GitHub `main` exposes, so row 3 now expects "not shipped" and row 11 (single-file TTS bundles) is new; the row 6 and 7 harnesses are named (row 6's replay script: round 2). The table is regrouped into 12 rows (row 12 = the NPU-zip rows split out of row 2); commands carry over and are re-checked in round 2, lanes are fixed in round 3. Each row is a verdict with its expected value and the command that measures it. Anything posted upstream is drafted first and goes out on GO. Commands re-checked 2026-09-26 (round 2): every command in the Command column is marked `checked` (file seen with `ls`, arguments read from its `--help`, usage line or source, `gh` and Hub API reads run against v0.17.1, `main` or the current Hub revision) or `not confirmed:` with the reason; no model, wheel or release asset was downloaded, nothing was built, and no device was used. The TTS/ASR half of the kit is [day0-0.18.0-tts-asr.md](day0-0.18.0-tts-asr.md).

Before step 1: `GEN_LITERTLM_TAG=v0.18.0`, `GEN_PYPI_LITERT_LM=0.18.0`, `GEN_MAVEN_LITERTLM_ANDROID=0.18.0`, `GEN_NPM_LITERTLM_CORE=0.18.0` in `pins.env`, then `rm -rf cache/ghlm` — `channels_common.download()` reuses a cached file whose size equals the asset size, so a same-size zip under a new tag would be read from the old bytes. Once 0.18.0 is on PyPI, make the venv that rows 3, 6 and 8–11 call: `/opt/homebrew/opt/python@3.14/bin/python3.14 -m venv ~/venvs/lt0180run && ~/venvs/lt0180run/bin/pip install litert-lm==0.18.0` (lt0171run's `pyvenv.cfg` records the same `python3.14 -m venv` call; `litert-lm` 0.17.1 pins `litert-lm-api` and `litert-lm-builder` of the same version, which bring `litert-lm-builder` and `litert-lm-peek`).

| # | Verdict | Expected (PASS =) | Command (re-checked 2026-09-26, round 2) | If not PASS | Lane (draft; fixed in round 3) |
|---|---|---|---|---|---|
| 1 | **Tag facts** (first 10 min): asset list with timestamps, tag commit, `Package.swift` pins at the tag, header set in the mac zip | Assets = `CLiteRTLM.xcframework.zip`, `CLiteRTLM_mac.xcframework.zip`, `litert_lm_main.macos_arm64`, `litert_lm_c_api-0.2.0.zip`, `CLiteRTLM.spdx.json`, `THIRD_PARTY_NOTICES.txt` (the v0.16.0 shape; the zip name follows `C_API_VERSION`, which was 0.1.0 on v0.16.0[^capi010]). Pins point at `download/v0.18.0/` (`main` pins the v0.17.1 zips today, sha256 `83efd536…` mac / `c94fc12a…` iOS[^pkg]). Mac zip `Headers/` = the seven public headers `c/BUILD` lists on `main` (the same seven at e2952685d[^cbuild]): `api_export.h`, `conversation.h`, `embedding_engine.h`, `engine.h`, `error_reporter.h`, `experimental.h`, `model_info.h`. `capabilities.h` (in the v0.17.1 zip) is gone: c1cf63794 renamed it to `model_info.h`. `api_export.h` is new (3468961f1, 09-17); `engine.h`, `experimental.h` and `error_reporter.h` include it, so a zip without it fails at the first include. `swift/` on `main` calls `model_info.h` and `error_reporter.h` | checked (gh 2.76.2; each call run against v0.17.1 on 09-26): `gh release view v0.18.0 --repo google-ai-edge/LiteRT-LM --json assets,publishedAt --jq '.'` (every asset carries `createdAt`) · `gh api 'repos/google-ai-edge/LiteRT-LM/git/ref/tags/v0.18.0' --jq .object` (v0.17.1 returns `type` `commit`; if `type` is `tag`, follow with `gh api repos/google-ai-edge/LiteRT-LM/git/tags/<sha> --jq .object.sha`) · `gh api 'repos/google-ai-edge/LiteRT-LM/contents/Package.swift?ref=v0.18.0' --jq .content \| base64 -d \| grep -n -A2 'url:'` · in `~/code/litertlm-release-gate`: `mkdir -p cache/tag-v0.18.0 && gh release download v0.18.0 --repo google-ai-edge/LiteRT-LM --dir cache/tag-v0.18.0`, then `unzip -l cache/tag-v0.18.0/CLiteRTLM_mac.xcframework.zip \| grep Headers/`. The tag's own download, because the gate's `cache/ghlm/` holds whatever `Package.swift` pins (the v0.16.0 zips on 09-09); `cache/` is gitignored, and rows 3 and 4 read the same dir | Each missing item is a row of [release-asset-checklist.md](release-asset-checklist.md); the tag-day table goes on the checklist issue (if open) or on the row's own issue | Mac (gh only) |
| 2 | **Gate 1b**: the GitHub-asset rows (`ghlm.*`; the `ghrt.*` rows are row 12), SwiftPM and README rows on the tag, then npm / Maven (phone attached), then the table and its diff against v0.17.0 | 0 new FAIL. Expected flips: `swiftpm.build` PASS only if row 1's pins are on v0.18.0; `quickstart.gemma4-*` FAIL→PASS (README fix 49cf34756 is on `main` since 09-04; PASS iff the cut is after it — v0.17.0 and v0.17.1 were cut on a branch without it[^readme]); `ghlm.c-api-zip` cell says *hosted on v0.18.0* (the runner falls back to older tags — a PASS on a v0.16.0 zip is not a 0.18.0 result); `ghlm.release-license-docs` FAIL→PASS if SBOM ships. `ghlm.xcf-capabilities.*` (two rows) go PASS→FAIL if the pinned zips carry the `c/BUILD` header set: the runner counts zip members matching `capabilit`, which the v0.16.0 zip met with `capabilities_c.h` and the v0.17.0 zip with `capabilities.h`, so `model_info.h` alone is the rename, not a regression; read row 1's header list before calling it one (`ghlm.c-api-capabilities` stays FAIL for the same reason)[^capgrep]. `maven.build` also depends on the AAR's Kotlin metadata (the 0.17.x consumer floor is Kotlin 2.3; #3753 and PR #3509 are open[^kotlin]). The SwiftPM and Maven generate legs pass on exit 0 + non-empty output, and a dropped prompt satisfies both (row 10): read the OUTPUT line for 42 (the prompt is "What is 17 + 25?") before recording PASS. The canary `gemma3-270m-it-q8.litertlm` carries no jinja template (structured `prompt_templates` only, header read 09-21), so the runtime's default path renders it | checked (usage lines of `run_channels.sh`, `make_table.py` and `diff_tables.py`; the channel runners take `--out DIR`, `maven_check.sh` also `--no-device`), in `~/code/litertlm-release-gate`: `./run_channels.sh --only gh_assets,swiftpm,quickstart` → phone attached → `./run_channels.sh --only npm,maven` (both write `results_$(date +%Y%m%d)/` and end with `make_table.py` on it, so run both on one calendar day) → `python3 make_table.py --results results_$(date +%Y%m%d) --round 2` (titles so far: round 0 = v0.16.1, round 1 = 0.17.0; writes `results/release_artifact_gate_<date>.md` and `…_data/`) → `python3 diff_tables.py ~/code/litertlm-convert/watch/on_release/channels/results_20260909 results/release_artifact_gate_$(date +%Y%m%d)_data`: two JSON dirs, not the `.md` (the 09-09 dir holds the v0.17.0 tag-day JSONs, 35 checks over the same five channels; the v0.17.0 table's 21 PyPI rows came from litertlm-convert's `qa/` gate and are not in it). Maven's device leg calls `adb` without `-s`: one phone attached, or `ANDROID_SERIAL` set; JDK = temurin-17, the runner's default. This checkout has no `cache/` yet, so the first SwiftPM resolve mirrors the repo (~3.2 GB, ~14 min, README). Not litertlm-convert `watch/on_release/run_on_release.sh --channels` or `--all`: those run litertlm-convert's own copy of these runners, which has diverged from this one and reads its own `pins.env`[^relgatecopy] | Every FAIL→PASS = a fix-confirmed comment seed (#3296 item 2, #3418, #3194, #2407 only with a fresh `--cache-path`); every PASS→FAIL = a regression comment the same day; a FAIL with no issue = new finding, draft only | Mac + Android CPU (Maven device leg) |
| 3 | **TTS/ASR shipped surface** — does any 0.18.0 artifact (xcframework ×2, AAR, wheel, CLI) expose `TtsEngine` / `AsrEngine` / `OmniEngine`? On `main` all of it is C++ under `omni/`: `TtsEngine` (Kokoro / Qwen3 TTS, since 7b0415bcf 08-31; Kokoro vocoder on GPU 7a01f227b 09-15), `AsrEngine`, and since 09-23 `OmniEngine` / `OmniSession`, which the ASR and TTS sessions now inherit[^omni] (`OmniStreamingSession`, added on 09-23, left `omni/omni_session.h` in f74763b54 on 09-24[^streaming]). `c/`, `swift/`, `python/` and `kotlin/` hold no Tts/Asr/Omni file; "Kotlin API for ASR" (3863a6aa5) reached GitHub as `jni_utils.h` only[^kotlinasr]. Open on 09-26: PR #3760 (AsyncStageScheduler 1 s wakeup stall, Kokoro acoustic padding)[^pr3760]; own `omni/asr` issues #3684 / #3685[^asrissues] | Not shipped (the default) = all of: in both xcframework binaries and the AAR's `liblitertlm_jni.so`, the engine pattern (`6litert4omni` = the mangled `litert::omni` namespace, `omni/` source paths, and the `Tts*` / `Asr*` / `Omni*` engine and session class names; Command column) = 0, with the zero control `FakeLlmExecutor\|FakeConstraint` = 0 and the `EmbeddingEngine` control > 0[^zero] (09-21 used the ASR strings as the zero control; ASR is now a target). `Kokoro` and `Qwen3Tts` are no longer part of it. Since 4cf0bf515 (09-22) the core loader parses the `TtsMetadata` / `AsrMetadata` enums, and `tts_model_type.proto` names two messages `Kokoro` and `Qwen3Tts`; a static library built on 09-25 from `main` at 66058c82 with the ASR engine only matches each of them 19 times (all protobuf symbols) and `TtsEngine\|TtsSession` 0 times. Those names are recorded as the metadata column, not read as shipped[^loaderproto]; no `tts`/`asr`/`audio`/`omni` header in `Headers/`; AAR `classes.jar` has no `*Tts*`/`*Asr*`/`*Omni*` classes; neither CLI has a tts or asr command (`litert_lm_main` has no tts/asr flag, the only `omni/` binary is `//omni/asr:asr_runner`, and 7b0415bcf's "and CLI tool" reached GitHub as nine `omni/tts/` sources with no CLI[^nocli]). The PyPI wheel is expected at 0 regardless (it never links `omni/`, RESULTS_TTS_WHEEL_0170). Any engine-pattern hit = shipped, the discovery case | checked (both scripts' usage lines and bodies; the patterns below were read against `main` e2952685d and counted on the cached v0.16.0 mac dylib): unpack into the layout the script reads (its lines 7–8 and 19–21): `X=~/code/litertlm-release-gate/cache/tag-v0.18.0; mkdir -p $X/xcf/CLiteRTLM_mac $X/xcf/CLiteRTLM && unzip -q $X/CLiteRTLM_mac.xcframework.zip -d $X/xcf/CLiteRTLM_mac && unzip -q $X/CLiteRTLM.xcframework.zip -d $X/xcf/CLiteRTLM` (zips from row 1) · the AAR, once row 2's Maven leg has resolved 0.18.0: `AAR=$(ls ~/.gradle/caches/modules-2/files-2.1/com.google.ai.edge.litertlm/litertlm-android/0.18.0/*/litertlm-android-0.18.0.aar)` (0.16.1 and 0.17.1 sit there on 09-26) · the script itself stays as it is; copy it: `D=~/code/litertlm-convert/watch/on_release/results_$(date +%Y%m%d); mkdir -p $D && cp ~/code/litertlm-convert/watch/on_release/results_20260909/tts_assets_check.sh $D/`, and in the copy add after line 16 `eng='6litert4omni\|omni/(asr\|tts\|base)/\|TtsEngine\|TtsSession\|AsrEngine\|AsrSession\|OmniEngine\|OmniSession\|OmniStreamingSession\|SpeechRecognizer'`, `meta='TtsMetadata\|AsrMetadata\|Kokoro\|Qwen3Tts\|Qwen3Asr\|Moonshine\|Parakeet\|Whisper'` and `zero='FakeLlmExecutor\|FakeConstraint'`, print `engine=`, `metadata=` and `zero=` counts in `probe()` (line 17) beside the existing ones, plus `6litert4omni3tts` and `6litert4omni3asr` counts to tell which engine came along (`asr_control` now reads as a target), change `probe()`'s `strings -a` to `strings -` (on a Mach-O file `-a` reads all sections but not the symbol table, `-` reads every byte, per `man strings`; on the v0.16.0 mac dylib 430,631 lines against 4,309,628, `EmbeddingEngine` 2 against 39), widen line 29's class-name grep to `tts\|asr\|omni\|speech\|audioout`, change `v0.17.0` to `v0.18.0` on line 33, then `bash $D/tts_assets_check.sh $X/xcf "$AAR" \| tee $D/tts_assets_check.log` (`javap` from temurin-17) · `~/code/litertlm-convert/watch/on_release/results_20260908/tts_wheel_check.sh 0.18.0 0.17.1 python3.12` (usage `[VERSION] [CONTROL_VERSION] [PYTHON]`; python3.12 = pyenv 3.12.13; it makes a throwaway venv under `/tmp/lt_tts_wheel_check_0.18.0`; its `omni` and ASR rows are the ship signal, its TTS row also matches `Kokoro\|Qwen3Tts`) · `~/venvs/lt0180run/bin/litert-lm --help \| grep -ciE 'tts\|asr\|speech\|synth'` (lt0171run: 0) · `ls $X/xcf/CLiteRTLM_mac/CLiteRTLM_mac.xcframework/macos-arm64_x86_64/Headers/ \| grep -ciE 'tts\|asr\|audio\|omni'` · if the tag ships `litert_lm_main.macos_arm64`: `strings - $X/litert_lm_main.macos_arm64 \| grep -cE` with the `eng` pattern (read it statically: its `--help` needs `libGemmaModelConstraintProvider.dylib` beside it, and dyld refuses the local 06-29 copy without one) · `omni/` binaries at the tag: `for p in $(gh api 'repos/google-ai-edge/LiteRT-LM/git/trees/v0.18.0?recursive=1' --jq '.tree[] \| select(.path\|test("^omni/.*BUILD$")) \| .path'); do echo "$p $(gh api "repos/google-ai-edge/LiteRT-LM/contents/$p?ref=v0.18.0" --jq .content \| base64 -d \| grep -c 'cc_binary(')"; done` (e2952685d: `omni/asr/BUILD` 1, the other five 0) | Shipped → run the Qwen3-TTS day-0 folder (`litertlm-convert/out/qwen3tts-day0/folder`, local, unpublished) through the shipped surface; PASS = ASR round-trip of the output WAV (faster-whisper) matches the input text; publication (HF files, card, collection) is a separate GO. The folder's file names are the `Qwen3TtsModelConfig` defaults on `main`, and a folder is detected by `talker*` or `codec_*` `.tflite` names[^qwen3dir]; five of its `.tflite` files are symlinks to an external archive disk (they resolved on 09-26), so mount it first. Not shipped → the folder stays local; add the 0.18.0 line to `qwen3tts_work/DAY0_FOLDER_FINDINGS.md`; the 0.18.0 ASR column then comes from `asr_runner` built at the tag commit (`bazel build //omni/asr:asr_runner`), run by edge-llm-bench `scripts/asr_rtf_mac.py` with `ASR_RUNNER_DIR` (steps: [day0-0.18.0-tts-asr.md](day0-0.18.0-tts-asr.md) §b) | Mac |
| 4 | **C API resumed?** (#3569, closed 09-15 as not planned: "We will release 0.18.0 very soon"[^3569]) | `litert_lm_c_api-0.2.0.zip` present with the v0.16.0 platform set (seven library files for six platforms) and the seven headers of row 1; renamed functions (whhone: "renaming some new functions") listed by a header diff against the v0.16.0 zip. 3468961f1 ("Define LiteRtLmStatusCode enum and return canonical status codes") landed after the draft, so the diff reads return types too, not names only. Since 5a34c6c40 the only public-header change on `main` is one new function, `int litert_lm_conversation_wait_until_done(LiteRtLmConversation*)` (e2952685d, 09-25, `conversation.h:501`), so the diff should list it[^wait] | checked (zip layout read from the cached v0.16.0 zip: `include/*.h`, `lib/<platform>/liblitert-lm.*`), in `~/code/litertlm-release-gate`: `unzip -l cache/tag-v0.18.0/litert_lm_c_api-0.2.0.zip \| grep -cE '\.(so\|dylib\|dll\|lib)$'` (v0.16.0: 7) · `unzip -q cache/tag-v0.18.0/litert_lm_c_api-0.2.0.zip -d cache/capi-v0.18.0`, and the v0.16.0 zip into `cache/capi-v0.16.0` (the local copy `~/code/litertlm-convert/watch/on_release/channels/cache/ghlm/litert_lm_c_api-0.1.0.zip` links into the external archive disk; otherwise `gh release download v0.16.0 --repo google-ai-edge/LiteRT-LM --pattern 'litert_lm_c_api-*' --dir cache/tag-v0.16.0`) · `diff <(grep -ho 'litert_lm_[a-z_]*' cache/capi-v0.16.0/include/*.h \| sort -u) <(grep -ho 'litert_lm_[a-z_]*' cache/capi-v0.18.0/include/*.h \| sort -u)` (v0.16.0: 2 headers, 144 names) · `nm -gU cache/capi-v0.18.0/lib/macos_arm64/liblitert-lm.dylib \| grep -c litert_lm_` · `grep -n LiteRtLmStatusCode cache/capi-v0.18.0/include/*.h` | Present → the rename list is what our Swift wrapper and bindings adapt to (no comment needed; the zip is the answer). Absent → record only (paused by their decision; a comment adds nothing) | Mac |
| 5 | **SwiftPM re-verification** (#3296): `exact: "0.18.0"` and `main` both build. On 09-23 nireekshak reproduced the `main` failure against the pinned v0.17.1 zips (missing `model_info.h` and `error_reporter.h`, plus newer `engine.h` / `embedding_engine.h` symbols) and is looking into the tag-ordering note[^3296] | Gate row `swiftpm.build` PASS at `exact:` (covered by step 2) **and** a `main` checkout builds against the v0.18.0 zips (item 2 of the 09-09 comment: `model_info.h` symbols) | checked: `exact:` = row 2's `swiftpm.build` (`swiftpm_check.sh` resolves `exact:` with a revision fallback and has no `main` mode) · `main`, by hand, once `main` re-pins (8ef538581 and 9465fc262 moved the pins about an hour after the 0.17 tags): `gh api 'repos/google-ai-edge/LiteRT-LM/contents/Package.swift?ref=main' --jq .content \| base64 -d \| grep -n 'download/'` shows `v0.18.0`, then `cd ~/code/litertlm-release-gate/cache && GIT_LFS_SKIP_SMUDGE=1 git clone --depth 1 https://github.com/google-ai-edge/LiteRT-LM lt-main && cd lt-main && swift build` (`Package.swift` sits at the repo root on e2952685d; the LFS skip is the runner's #2407 workaround; host on 09-26: Xcode 27.0 (27A266a) / Swift 6.4; the v0.17.0 rows were measured on Xcode 27 beta 5) | Both PASS → close-out comment on #3296 (fix confirmed, both items). `exact:` FAIL → comment the same day with the runner's missing-symbol list (the checklist row 1 case). `main` FAIL only → one line on #3296 naming the header | Mac (iOS build only) |
| 6 | **#3444 re-verification**, two shapes (closed 09-15 "completed"; 91a926c16 clamps `max_num_tokens` against static capacity, dc911c3db errors at signature selection). Shape 1 = the reported wall at ~2048 mid-conversation. Shape 2 (own 09-18, after the close): `qwen3_0_6b_mixed_int4` (a 2,048-entry cache) with `maxNumTokens: 8192` on the v0.16.0 macOS GPU path completes a 1,987-token prompt + 256-token reply at full speed while logging `Invalid decode and sample result…` 10,666 times[^3444] | PyPI path: the 09-06 probe result unchanged (five 785-token turns, `token_count` 3926, turn 6 fails at the real 4096 wall with `remaining capacity`) — no regression. xcframework/Metal path, shape 1: with `maxNumTokens: 4096` on gemma-4-E2B, no mid-conversation `Chosen prefill work group size exceeds available state entries`; either the full budget is served or init reports the ceiling. Shape 2: the same Qwen3-0.6B run is clamped or rejected at init, or fails with an error — no `Invalid decode and sample result` line | checked: PyPI path `~/venvs/lt0180run/bin/python ~/code/hf-to-litertlm/tools/runtime_probes/3444_context_ceiling/probe_3444_ceiling.py ~/.cache/huggingface/hub/models--litert-community--gemma-4-E2B-it-litert-lm/snapshots/b3ca0d2f076785a8f4b2219ddbd2bdb99954eae1/gemma-4-E2B-it.litertlm`, then the same with a trailing `gpu` (the bundle argument is required, per the docstring; the file sits in the HF cache, 2,588,147,712 B) · Metal build, in `~/code/edge-llm-bench` (`docs/OPERATIONS.md:34-49`): `mv ios/BenchmarkApp/Vendored/LiteRT-LM ios/BenchmarkApp/Vendored/LiteRT-LM.v0160` (v0.16.0 today; `bootstrap.sh:79` clones only when that dir is absent), `LITERTLM_TAG=v0.18.0 ios/BenchmarkApp/scripts/bootstrap.sh`, `DD_MAC=.build/dd-mac-0180 scripts/build_yardstick_mac.sh` (it prints the binary: `YS=.build/dd-mac-0180/Build/Products/Release/yardstick`); the working tree had uncommitted edits to `project.pbxproj` and `MediaPipeRuntime.swift` on 09-26, so read `git status --short` before building. Bootstrap applies `ios/BenchmarkApp/patches/litert-lm-swift-benchmark-maxnumtokens.patch` (`bootstrap.sh:95`, relative to `ios/BenchmarkApp/`), whose preimage is `swift/Benchmark.swift` blob `a08eecad`, the same blob at v0.16.0, v0.17.1 and e2952685d; `gh api 'repos/google-ai-edge/LiteRT-LM/contents/swift/Benchmark.swift?ref=v0.18.0' --jq .sha` shows whether the tag still has it (not confirmed until the tag exists) · shape 1 = the endurance harness that produced #3444 (task `endurance-chat-30m`, `matrices/endurance-mac.cells`; that cell pins gemma-4-E2B at `context-tokens=2048` since the 09-01 wall, so the replay passes 4096), with `<dir>` = `results/raw/$(date +%Y-%m-%d)-3444-v0180-m4max-mac` (`mkdir -p` it first): `$YS run --runtime litert-lm --model-id litert-community/gemma-4-E2B-it-litert-lm --task endurance-chat-30m --runs 1 --context-tokens 4096 --output <dir>/shape1.jsonl` (30 min), then `grep -c '"rolloverReason":"kv-wall@' <dir>/shape1.turns.ndjson` (the per-turn sidecar is `<output>` with `.jsonl` replaced, `Yardstick.swift:294-297`; the harness rolls the conversation over on the wall and records `kv-wall@<kv>: <error>`, budget rollovers read `"budget"`) · shape 2 = direct yardstick launches, one per probe (`probe-evidence/README.md`: one launch each, `--runs 2`; `matrices/dashboard-longctx-v1.cells:53-54` keeps these cells as `exclude=`): p7 `$YS run --runtime litert-lm --model-id litert-community/Qwen3-0.6B --task long-context-2048-gen256 --litert-backend gpu --context-tokens 8192 --runs 2 --output <dir>/p7.jsonl.probe 2> <dir>/p7.log`, p5 the same with `--context-tokens 2304`, p9 with `--task long-context --context-tokens 4096`; `grep -c 'Invalid decode and sample result' <dir>/p7.log` (09-18, v0.16.0: 10,666; p5 and p9: 0). `--litert-backend` and `--context-tokens` are parsed in `apple/YardstickCLI/Sources/Yardstick.swift:111,130` but not listed by `yardstick --help`; model ids and tasks come from `yardstick list` and the probe records[^harness3444] | Fixed on Metal → fix-confirmed comment on #3444. Wall still at ~2048 → comment with the turn number and message, same shape as the original. Shape 2 still silent → add the log count to that comment (the 09-18 note offered a re-check on the next tag) | Mac + iOS |
| 7 | **#3577 re-verification** (open; nireekshak reproduced 09-16, CPU and macOS GPU accurate, Adreno under investigation. Own second data point 09-24: on 144 three-choice prompts, `Backend.GPU()` and `Backend.CPU()` greedy answers differ on 26 with MiniCPM5-2B int8 (0.17.1) and 23–24 with Qwen3-0.6B (0.16.1 / 0.17.1), with the question whether the int8 flips are this issue — unanswered on 09-26[^3577]) | Same bundle (`dynamic_wi4c_hr_afp32`, 1,306,168,768 B, sha256 `6561646e…`) on `litertlm-android:0.18.0`, Galaxy S26, greedy, 8 questions: GPU answers match CPU on ≥ 7/8 = fixed; 1/8 as before = unchanged. No fix was promised for 0.18.0, so unchanged is the expected value. Same harness, with the APK rebuilt against the 0.18.0 AAR (the installed APK was built 09-10 against the 0.17.0 AAR) | checked: APK rebuild (edge-llm-bench `android/ddp-bench/README.md:15-26`; the pin is `gradle.properties:8` `litertlmVersion=0.17.0`, the default in `app/build.gradle.kts:10`, overridable). Build from a clean worktree: the working tree had uncommitted edits to `Gate.kt` / `LitertlmBenchTest.kt` on 09-26 (the `Gate.kt` one appends channel text to the answer the gate reads), and the committed app sources are unchanged since c906a6e, the last ddp-bench commit before the installed APK (09-10 11:50; APK 11:53): `git -C ~/code/edge-llm-bench worktree add ~/code/edge-llm-bench-apk0180 c906a6e && cd ~/code/edge-llm-bench-apk0180/android/ddp-bench && JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home ./gradlew --no-daemon -PlitertlmVersion=0.18.0 :app:assembleDebug :app:assembleDebugAndroidTest` (`ANDROID_HOME` is set in the shell, so no `local.properties`) · witness: `unzip -p app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk assets/engine-pin.json` must read `0.18.0` (the 09-10 build reads `0.17.0`, AAR `28aa6bc4…`) · install as `run_local.sh:65-66` does: `adb -s RFGL80R6A6H install -r -t app/build/outputs/apk/debug/app-debug.apk`, then the same for `app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk` · driver, in litertlm-convert `minicpm52b_work/teamrecipe/clab/` (usage from its docstring; serial `RFGL80R6A6H` hard-coded; the device hold belongs to the caller's sleeper pid): `ROWS=rows_aar0180.json python3 s26_aar0170.py r0180_int4hr=/data/local/tmp/aar0170/readme_int4hr.litertlm --backends gpu,cpu --reps 2` (the 09-13 form, `logs/runlog.txt:93`; per-run outputs land in `clab/s26_0170/<tag>_<backend>_r<rep>`, so a new tag keeps the 0.17.0 ones)[^driver3577] · not confirmed: the bundle. The Mac copy `teamrecipe/out_int4hr/model.litertlm` (sha256 `6561646e…`) is a known-lost self-link on the archive disk, and the S26 copy under `/data/local/tmp/aar0170/` was not checked (no adb this round). Regenerating it is `teamrecipe/export_teamrecipe.sh dynamic_wi4c_hr_afp32 <new dir>` (venv `~/venvs/lt094dev`; `openbmb/MiniCPM5-2B` is in the HF cache), then compare the sha256 before use[^bundle3577] | Changed either way → one comment on #3577 with the 8-question table. Unchanged → nothing (they are investigating) | Android GPU |
| 8 | **LFM2.5 regate**: do the shipped hybrid (ShortConv) bundles still run, and does GPU now start? `main`'s LiteRT pin 26895c9f contains the `short_conv_step` fusion (#9858, merged 09-10 as b11b1b0ef); the v0.17.0 / v0.17.1 pin 9fe5be45 does not[^9858] | Mac: all nine `models.tsv` rows run[^tsv]. The verdict reads the five LFM2.5 rows (lfm25-12b ×4, lfm25-26b): q8 scores equal the 08-11 lines (lfm25-12b-int4-shipped 7/8 cpu, lfm25-12b-int4-gpu 7/8 gpu, lfm25-26b-int4 8/8 cpu); GPU decode within ±2 % of the 2026-08-11 baselines in `models.tsv`. granite-350m, bitcpm-1b and ministral-3b: runs, no regression. qwen35-08b is row 9. iPhone 17 Pro Metal gate (NEXT-SESSION-lfm25-gpu-regate.md §1, v0.18.0 xcframework checksum): GO = engine PASS + 8Q GPU ≥ CPU + decode or TTFT above CPU (52.6 tok/s / 442 ms) | checked: first `gh api 'repos/google-ai-edge/LiteRT-LM/contents/WORKSPACE?ref=v0.18.0' --jq .content \| base64 -d \| grep -n 'LITERT_REF ='` (e2952685d: line 6, `26895c9f…`), then `gh api 'repos/google-ai-edge/LiteRT/compare/b11b1b0efde5e38a7f671348ffeb7c62c7bd05d1...<LITERT_REF>' --jq .status` (`ahead` = #9858 is inside the pin) · `RUN_VENV=~/venvs/lt0180run ~/code/litertlm-convert/watch/on_release/runtime_regate.sh --quick`, then `--no-gpu` (usage from the script's header, lines 8–10, and its argument parser, lines 28–30: `--quick` = the Mac GPU gate only, `--no-gpu` = the quality gates only, `--only <tag>` = one row; `SUMMARY.txt` in `watch/on_release/results_<date>/`; serial, quiet Mac; `run_on_release.sh --runtime` would run `--quick` and then a full pass instead) · iPhone per litertlm-convert `watch/NEXT-SESSION-lfm25-gpu-regate.md` §1 (USB transfer, stale-pcm cleanup; harness `~/code/apple-silicon-llm-bench`; the v0.18.0 zip's checksum: `swift package compute-checksum <zip>`) · not confirmed: the inputs. All nine `models.tsv` paths are absent under `~/code/litertlm-convert` on 09-26 (none of their directories holds a `.litertlm`; the one file found, `bitcpm-cann-1b_wi4b32_wi8.litertlm`, sits on the external archive disk under `archive/litertlm-convert/bitcpm_work/`, not at the manifest path). The runner prints `SKIP <tag> (missing: …)` per row and exits normally, so it gates nothing until the files are restored or a `MANIFEST=` copy points at restored paths; the note's §1 names three of the same files[^tsvmissing] | GPU GO → the re-ship path in the note §2 (Instruct / JP / Thinking; user GO per file). FAIL → PARKED_WALLS row 21 gets the 0.18.0 signature diff; #3129 follow-up drafted | Mac + iOS |
| 9 | **Qwen3.5 regate**: gated-delta (GDN) bundles still run on CPU; record GPU | `qwen35-08b-int8` 8/8 cpu (same run as step 8). GPU on the shipped Qwen3.5-0.8B int8: record delegation count and the 8 answers — no expected value (GDN on GPU is a converter-side wave, litert-torch #1233/#1234, both merged 09-15 / 09-16[^gdn], not a runtime switch). MTP drafter e2e on 0.18.0: acceptance equal to the recorded runs in `mtp_work/` | checked: the 8 GPU answers, `LITERT_LM=~/venvs/lt0180run/bin/litert-lm ~/venvs/lt0180run/bin/python ~/code/litertlm-convert/watch/on_release/gate8q.py <file> gpu <out.json>` (usage `gate8q.py <model> <cpu\|gpu> [out.json]`), with `<file>` = `litert-community/Qwen3.5-0.8B` `Qwen3.5-0.8B_int8.litertlm` at the current revision (03d910d3, the 09-21 template-swap upload, LFS sha256 `64ed396f…`, 963,184,864 B; no local copy); `litert-lm run --from-huggingface-repo litert-community/Qwen3.5-0.8B Qwen3.5-0.8B_int8.litertlm --backend gpu --prompt '…'` fetches it (flags from lt0171run's `run --help`) · MTP: `python3 ~/code/litertlm-convert/mtp_work/e2e_ab.py <drafter bundle> --json <out.json>` from a copy whose line 25 points `CLI` at `~/venvs/lt0180run/bin/litert-lm` (hard-coded to lt0160run; the script has no flag for it)[^mtp] · not confirmed: step 8's CPU row (its `models.tsv` file is absent too); how to read a delegation count from the Mac CLI (no Mac `litert-lm` log under `qwen35_work/` or `watch/on_release/results_*` prints one, the Android logs print `Replacing N out of M node(s) with delegate`); the 0.8B drafter `mtp_work/out_08b/Qwen3.5-0.8B_mtp_drafter_int8.litertlm` recorded in `e2e_08b.json` (absent; the `topk32k` 0.8B and both 2B drafters resolve into the archive disk) | Any CPU regression → the same-day regression comment. GPU numbers → the dashboard row kit (separate candidate), not a comment | Mac |
| 10 | **Chat template standard** (`models/README.md`: `content` is a list of typed parts; own #3688, whhone 09-20 "the contents will always be a list of object with a type"; closed by whhone 09-22 with "Please reopen if it is not resolved"[^3688]) | The tag carries `models/README.md` and `models/chat_template_input_schema.json` with `content` as a list of parts, and 6c6b4582 (content normalized to parts) is in it. On `litert-lm==0.18.0`: the 53 own bundles whose template was swapped (37 repos, TFLite bytes unchanged, marked uploaded by the owner) answer on-prompt; the regress set is the 9 HOLD files (not swapped) and other publishers' string-only bundles (no inventory: the audit covered own files only[^audit]), each in the class the audit predicted for the parts form[^r3pred] — S-DROP answers another question (exit 0), S-RAISE raises, S-LEAK leaks the parts container into the prompt and may still answer on-prompt; dual-form, parts-only and no-template bundles answer the question asked. The own-bundle audit (2026-09-21, `~/code/codex-conversions/2026-09-21/template-parts-audit/`, DONE 09-22 at round 10 and folded on the owner's GO the same day) totals 43 repos / 62 files (53 READY + 9 HOLD); its round 3 pair (REL 0.17.1 / NIGHTLY 0.18.0.dev20260919) ran 7 bundles, one per class (qwen3_06b S-DROP, falcon_h1 S-RAISE, lfm25_12b S-LEAK, qwen35_08b S-LEAK, lfm25_230m DUAL, granite42_3b PARTS, sarashina22_05b NONE), 14 rows. Prompt = "What is the capital of France?"; the verdict is read from the text, never from the exit code | checked: `gh api 'repos/google-ai-edge/LiteRT-LM/contents/models/README.md?ref=v0.18.0' --jq .sha` and the same for `models/chat_template_input_schema.json` (both on e2952685d) · `gh api 'repos/google-ai-edge/LiteRT-LM/compare/6c6b4582...v0.18.0' --jq .status` (`ahead` = included, e2952685d is `ahead` by 144; `diverged` = release branch, grep `runtime/` for the normalization instead) · the set, fixed in round 2: the 7 round-3 bundles (`results/round3/selection.json`) at the current Hub revision. The smallest swapped file per class among the 53 in `SHIPMENT.md` is already one of them (S-DROP `Qwen3-0.6B_dynamic_wi4b32_afp32_9flags.litertlm`, 341,424,096 B, line 295; S-RAISE `Falcon-H1-0.5B-Instruct_int8.litertlm`, 650,163,952 B, line 197; S-LEAK `LFM2.5-1.2B-Instruct_int4.litertlm`, 736,015,744 B, line 21, the same size as the Thinking and JP int4 files and first in file order), so the day downloads 7 files, 5,592,742,160 B. On 09-26 four of them carry the swapped template (Hub LFS sha256 = the `SHIPMENT.md` sha: qwen3_06b, falcon_h1, lfm25_12b, qwen35_08b) and three are the round-3 bytes (lfm25_230m, granite42_3b, sarashina22_05b), so none of the 7 is string-only any more[^r3today] · per bundle (all 7 repos public, ungated): `REV=$(curl -s https://huggingface.co/api/models/<repo> \| jq -r .sha)`, `curl -fL -o <dir>/<file> "https://huggingface.co/<repo>/resolve/$REV/<file>"` (URL form checked with a HEAD request), `shasum -a 256 <dir>/<file>` against the Hub LFS sha, then `~/venvs/lt0180run/bin/litert-lm run <dir>/<file> --backend cpu --top-k 1 --temperature 0 --prompt 'What is the capital of France?' < /dev/null` (the round-4 rerun form: without `--max-num-tokens 512`, which made Qwen3 fail prefill on both runtimes in round 3; flags from lt0171run's `run --help`) · not confirmed as day commands: `scripts/round3_download.py <bundle>` and `scripts/round3_run_pair.py <bundle>` (codex-conversions `2026-09-21/template-parts-audit/`). Both stop at a deadline guard (`results/round3/config.json` `deadline_utc` = 2026-09-21T04:39:35Z); the download asserts the 09-21 pinned revision; the pair runs `.venv-rel` then `.venv-nightly`, neither of which exists now (only `.venv`), and refuses existing logs | Prediction holds → the 9 HOLD files stay the remaining re-ship candidates (each HOLD reason still applies), one GO per file. An S-DROP or S-RAISE bundle answers on-prompt → the runtime kept a string path: re-read the data processor before re-shipping anything. A swapped, dual-form or parts-only bundle fails → same-day comment on #3688 with the rendered prompt | Mac (Python) |
| 11 | **Single-file TTS bundle** (new 09-26): does the tag ship a writer for `TtsMetadata` (builder / peek); the Kokoro-type section layout; how own TTS assets would be re-packed (draft) | No writer, as on `main` today: `LitertLmFileBuilder` has no TtsMetadata adder, its `TfLiteModelType` has 15 values and no acoustic / vocoder, and `add_tflite_model` refuses a `model_type` override; `litertlm_peek` and the builder CLI have no tts/asr[^builder]. The reader side is on `main`: the C++ loader reads `TtsMetadataProto` / `AsrMetadataProto` sections (4cf0bf515) and takes the TtsMetadata enum names as a section's `model_type`[^loader]. Kokoro from one `.litertlm` (b5bc8faf8[^kokoro]): TFLiteModel sections `tf_lite_acoustic` + `tf_lite_vocoder`; GenericBinaryData `espeak-ng`, `<lang>-textnorm` (e.g. `cmn-textnorm`), `<lang>-lexicon` (e.g. `ja-lexicon`), and every other GenericBinaryData name is a voice pack (`voices/af_heart.bin` → `af_heart`); the model type comes from `TtsMetadata.tts_model_type` (kokoro / qwen3_tts), else Kokoro if an acoustic section exists; a folder without a `.litertlm` is detected by file names. So today's builder API has no way to write the two sections a Kokoro file needs. Qwen3-TTS still loads from a folder only[^qwen3folder]. A writer at the tag (a TTS adder, acoustic / vocoder in the enum, or peek printing TtsMetadata) = the discovery case | checked on lt0171run (swap in lt0180run on the day): `~/venvs/lt0180run/bin/litert-lm-builder --help \| grep -ciE 'tts\|asr\|acoustic\|vocoder\|generic'` (0.17.1: 0; subcommands `toml` … `unpack`) · `~/venvs/lt0180run/bin/litert-lm-builder tflite_model --help \| grep -ciE 'acoustic\|vocoder'` (0.17.1: 0; 15 `--model_type` choices) · `~/venvs/lt0180run/bin/python -c "from litert_lm_builder import litertlm_builder as b; print([m.name for m in b.TfLiteModelType]); print([n for n in dir(b.LitertLmFileBuilder) if n.startswith('add_')])"` (0.17.1: 15 names; nine adders, `add_generic_binary_data` among them, none for TTS) · `~/venvs/lt0180run/bin/litert-lm-peek --help` (0.17.1: `--litertlm_file`, `--dump_files_dir`) · row 3's `omni/` `cc_binary` loop · assets, gaps and signals: [day0-0.18.0-tts-asr.md](day0-0.18.0-tts-asr.md) | Writer shipped → draft the re-pack of own TTS assets (the Qwen3-TTS day-0 folder also needs a Qwen3-TTS single-file loader; own TTS assets and what each lacks: [day0-0.18.0-tts-asr.md](day0-0.18.0-tts-asr.md) §c); draft only, each upload its own GO. No writer → the 0.18.0 line goes into litertlm-convert `qwen3tts_work/DAY0_FOLDER_FINDINGS.md`, next to row 3's | Python builder + Mac |
| 12 | **NPU runtime zip** (split out of row 2 on 09-26): the gate's `ghrt.*` rows — vendor dirs inside LiteRT's `litert_npu_runtime_libraries.zip` and `litert_npu_runtime_libraries_jit.zip` at `GEN_LITERT_TAG` (v2.2.0, still LiteRT's latest release on 09-26) | `ghrt.npu-vendors.*` as in the v0.17.0 generation: `qualcomm` present, `samsung` / `mediatek` missing, FAIL on both zips[^ghrt]. LiteRT#9482 (not ours) is open: on 08-24 a maintainer asked about the Samsung / MediaTek license, and a 09-21 follow-up has no answer[^9482]. PASS needs a LiteRT release that carries all three vendor dirs | checked: `gh release view --repo google-ai-edge/LiteRT --json tagName,publishedAt --jq '.tagName + " " + .publishedAt'` (09-26: `v2.2.0 2026-08-13T17:30:00Z`); a newer tag moves `GEN_LITERT_TAG` in `pins.env` before row 2 · the rows come from row 2's `gh_assets` run (`gh_assets_check.py` matches `qualcomm` / `samsung` / `mediatek` in each NPU zip's member names); read them in `~/code/litertlm-release-gate` with `python3 -c 'import json,sys; [print(c["id"], c["status"], c["measured"]) for c in json.load(open(sys.argv[1]))["checks"] if c["id"].startswith("ghrt.")]' results_$(date +%Y%m%d)/gh_assets.json` (on the 09-09 JSON: both `ghrt.npu-vendors.*` FAIL, qualcomm present) | Unchanged → record only (someone else's report; a +1 adds nothing). Vendor dirs appear → the flip goes in the table diff; no comment needed | Mac (gh only) |

## Outputs of the day

1. `results/v0.18.0-generation.md` (table + diff at the top, steps 1, 2 and 12) and `pins.env` moved — committed.
2. Comment seeds, each its own GO: #3296 (step 5), #3444 (step 6), #3577 only if changed (step 7), the checklist issue with the tag-day table (step 1), #3129 if LFM2.5 GPU moved (step 8).
3. TTS decision (steps 3 and 11) and the LFM2.5 GPU verdict (step 8) written back to `DAY0_FOLDER_FINDINGS.md` and `PARKED_WALLS.md` row 21; if step 3 finds TTS/ASR not shipped, the asr-rtf 0.18.0 column in edge-llm-bench `results/raw/` ([day0-0.18.0-tts-asr.md](day0-0.18.0-tts-asr.md) §b).
4. Template verdicts (step 10) written into the audit's list; each re-ship is its own GO.

Rough wall-clock: steps 1–2 and 12 about 45 min (SwiftPM mirror and README downloads dominate; step 12 is part of the same `gh_assets` run), 3–5 about 30 min, 6 about 30 min, 7 about 1 h (device) plus the APK rebuild (not timed), 8–9 about 2 h (serial Mac runs plus the iPhone gate), 10 about 30 min (7 downloads, 5.6 GB, CPU; round 2 added no bundle, see the row), 11 not timed. Also not timed: step 7's bundle regeneration if the S26 copy is gone, and step 8's file restore, without which step 8 gates nothing (Command column). One session.

## Sources (re-read 2026-09-26)

Commit SHAs below were checked with `gh api` on 2026-09-26. "Local" = a file on the maintainer's machine, not a URL.

[^main]: `main` at e2952685d (2026-09-25T22:25Z): https://github.com/google-ai-edge/LiteRT-LM/commit/e2952685dd50c6ed24f067849a218d6249641c7b · 45 commits after 5a34c6c40 (compare status `ahead`, `ahead_by` 45): https://github.com/google-ai-edge/LiteRT-LM/compare/5a34c6c404b9c5ca57c2017f061b7d5ee0c8f59f...e2952685dd50c6ed24f067849a218d6249641c7b
[^nightly]: `litert-lm-nightly` 0.18.0.dev20260923, uploaded 2026-09-24T06:40Z: https://pypi.org/project/litert-lm-nightly/0.18.0.dev20260923/ (release channel unchanged: `litert-lm` 0.17.1, 2026-09-15T22:03Z).
[^litertref]: Set by 6a9af909a (2026-09-25T16:44Z; before it, ccff7848 from 02e503079): https://github.com/google-ai-edge/LiteRT-LM/commit/6a9af909aa4b4f16179280e037e7f9c9489bef7d
[^qairt]: QAIRT 2.50, 282e61d66 (2026-09-25T01:22Z): https://github.com/google-ai-edge/LiteRT/commit/282e61d661376d6eea87ff6fe5f9588471e431f9 · QAIRT 2.49, e156fd3e1 (2026-09-04T00:18Z): https://github.com/google-ai-edge/LiteRT/commit/e156fd3e19beb06228a88841773599d581e633fe · 26895c9f is 3 commits after 282e61d66.
[^capi010]: v0.16.0 shipped `litert_lm_c_api-0.1.0.zip` (https://github.com/google-ai-edge/LiteRT-LM/releases/tag/v0.16.0), and `version.bzl` at that tag has `C_API_VERSION = "0.1.0"`; `main` has 0.2.0.
[^pkg]: 9465fc262 (2026-09-16): https://github.com/google-ai-edge/LiteRT-LM/commit/9465fc262a44b2fc56125e4ae6c55fff85cef464
[^cbuild]: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/c/BUILD#L28-L42
[^readme]: Both compares are `diverged`: the tags sit 1 and 2 commits on a branch that lacks the 61 `main` commits up to 49cf34756. https://github.com/google-ai-edge/LiteRT-LM/compare/49cf347566cd09004746a3f40c3e80dbb25f8b41...e9fd8c53ff968071774206163027dd84bedfe925 · https://github.com/google-ai-edge/LiteRT-LM/compare/49cf347566cd09004746a3f40c3e80dbb25f8b41...5e58e9a0aef7abf7091207a8b1d1063a1c800f08
[^kotlin]: This repo, 5869b0f: https://github.com/john-rocky/litertlm-release-gate/commit/5869b0ff2f381876ee6b20721e133665915a28e7 · #3753: https://github.com/google-ai-edge/LiteRT-LM/issues/3753 (last comment 09-25: https://github.com/google-ai-edge/LiteRT-LM/issues/3753#issuecomment-5832725494) · PR #3509: https://github.com/google-ai-edge/LiteRT-LM/pull/3509
[^omni]: a327b494f, OmniEngine / OmniSession: https://github.com/google-ai-edge/LiteRT-LM/commit/a327b494f874a319605e6fd7e3439678daa4d07d · 62839a736, OmniStreamingSession: https://github.com/google-ai-edge/LiteRT-LM/commit/62839a736917ee862402bff5ca3d5ca382c52c83 · f74763b54, sessions inherit OmniSession: https://github.com/google-ai-edge/LiteRT-LM/commit/f74763b5420b61b83441eed103305d1cbdd72249 · a2cab0fdf, API cleanup: https://github.com/google-ai-edge/LiteRT-LM/commit/a2cab0fdf31db468017f10858b43ec5f994994f7 · also since 5a34c6c40: Kokoro Mandarin 42febfbfd / b8a4e4c81, `AudioInput` split 66058c82e, audio adapter on its own backend ea2b782ea.
[^kotlinasr]: The message names Kotlin `AsrEngine` / `AsrSession` / `AudioSource` and a `litertomni.cc` split; the GitHub diff is `kotlin/java/com/google/ai/edge/litertlm/jni/jni_utils.h` (+65 −3) only: https://github.com/google-ai-edge/LiteRT-LM/commit/3863a6aa5c1c38b836d05b03afa31be0ff52cf33
[^pr3760]: https://github.com/google-ai-edge/LiteRT-LM/pull/3760 (opened 09-25, not merged on 09-26).
[^asrissues]: #3684: https://github.com/google-ai-edge/LiteRT-LM/issues/3684 (last comment 09-21: https://github.com/google-ai-edge/LiteRT-LM/issues/3684#issuecomment-5767078790) · #3685: https://github.com/google-ai-edge/LiteRT-LM/issues/3685 (last comment 09-21: https://github.com/google-ai-edge/LiteRT-LM/issues/3685#issuecomment-5767636531)
[^nocli]: 7b0415bcf's GitHub diff is 9 files, all under `omni/tts/`: https://github.com/google-ai-edge/LiteRT-LM/commit/7b0415bcf36c13b5a4cda1d6f07df23c5abc1993 · at e2952685d, `runtime/engine/litert_lm_main.cc` has no tts/asr string, `omni/tts/BUILD` has no `cc_binary`, the one `omni/` binary is https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/asr/BUILD#L624-L625, and the Python CLI's commands (benchmark, convert, delete, describe, import, list, pack, rename, run, serve, unpack) have no tts/asr.
[^qwen3dir]: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/tts/qwen3_tts/qwen3_tts_model_config.h#L27-L43 · https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/tts/tts_engine.cc#L139-L146
[^3569]: https://github.com/google-ai-edge/LiteRT-LM/issues/3569#issuecomment-5688042251 (issue state reason: not planned).
[^wait]: https://github.com/google-ai-edge/LiteRT-LM/commit/e2952685dd50c6ed24f067849a218d6249641c7b · in the 5a34c6c40...e2952685d compare, `c/conversation.h` (+16) is the only changed public header; `swift/` does not call the new function.
[^3296]: https://github.com/google-ai-edge/LiteRT-LM/issues/3296#issuecomment-5797714254
[^3444]: https://github.com/google-ai-edge/LiteRT-LM/issues/3444#issuecomment-5723154975
[^harness3444]: Local: edge-llm-bench `docs/OPERATIONS.md:34`, `ios/BenchmarkApp/scripts/bootstrap.sh:28` (`LITERTLM_TAG` default v0.16.0), and the probe-evidence dir (all present on 09-26). The logs are public at the link in the 09-18 comment: https://github.com/john-rocky/edge-llm-bench/tree/main/results/raw/2026-09-18-dashboard-longctx-v1-m4max-mac/probe-evidence · Round 2 (09-26, edge-llm-bench HEAD 7d74707): shape 2 = `probe-evidence/README.md` (yardstick built 09-18 05:00 from that tree at the v0.16.0 pin, one launch per probe, `--runs 2`), `p7.jsonl.probe` / `p5.jsonl.probe` (task `long-context-2048-gen256`, `contextTokensConfigured` 8192 / 2304) and `p9.log` (task `long-context`, `FAILED_PRECONDITION … remaining capacity: 2048`); shape 1 = `matrices/endurance-mac.cells:11` (the 09-01 smoke's turn-21 wall under a 4096 request) and `results/raw/2026-09-01-mac-litert-endurance-v0160/`; rollover reasons are set in `ios/BenchmarkApp/Sources/Runtimes/MediaPipeRuntime.swift:294` (`budget`) and `:384` (`kv-wall@…`).
[^3577]: https://github.com/google-ai-edge/LiteRT-LM/issues/3577#issuecomment-5808652790 (nireekshak's 09-16 reproduction: https://github.com/google-ai-edge/LiteRT-LM/issues/3577#issuecomment-5701666673)
[^driver3577]: Local: litertlm-convert `minicpm52b_work/FINDINGS.md:473` (the 09-13 run on the 0.17.0 AAR) and the docstring of `s26_aar0170.py` (APK built 2026-09-10 against `litertlm-android:0.17.0`, sha256 `28aa6bc4…`). `s26_kit.py` in the same dir drives the v0.16.0 CLI kit, not the APK.
[^9858]: #9858 merged 2026-09-10 as b11b1b0ef: https://github.com/google-ai-edge/LiteRT/pull/9858 · b11b1b0ef...26895c9f is `ahead` (208): https://github.com/google-ai-edge/LiteRT/compare/b11b1b0efde5e38a7f671348ffeb7c62c7bd05d1...26895c9fbcc25c43faa8c1a98cd1fd28951602c3 · b11b1b0ef...9fe5be45 (`LITERT_REF` at v0.17.0 and v0.17.1) is `behind` (170): https://github.com/google-ai-edge/LiteRT/compare/b11b1b0efde5e38a7f671348ffeb7c62c7bd05d1...9fe5be45564c868408e6514c8aabb83e211a0911
[^tsv]: Local: litertlm-convert `watch/on_release/models.tsv`, 9 data rows on 09-26: lfm25-12b-int4-shipped, lfm25-12b-int4-gpu, lfm25-12b-wi8-gpu, lfm25-12b-wi8-composite, lfm25-26b-int4, qwen35-08b-int8, granite-350m-int8, bitcpm-1b-int4, ministral-3b-int4.
[^gdn]: https://github.com/google-ai-edge/litert-torch/pull/1233 · https://github.com/google-ai-edge/litert-torch/pull/1234
[^mtp]: Local: litertlm-convert `mtp_work/e2e_ab.py` (present on 09-26; the 09-21 text said "`mtp_work/` e2e script"). Round 2: its line 25 hard-codes `~/venvs/lt0160run/bin/litert-lm`; of the four drafters named in `mtp_work/e2e_*.json`, `out_08b/Qwen3.5-0.8B_mtp_drafter_int8.litertlm` is absent and the other three are links that resolve into the external archive disk.
[^3688]: https://github.com/google-ai-edge/LiteRT-LM/issues/3688#issuecomment-5770513843
[^audit]: Local, in the audit dir: `STATUS.md` (DONE 2026-09-22, round 10); `SHIPMENT.md` (53 READY rows marked uploaded, in 37 repo sections; line 3: 53 READY / 9 HOLD, 62 receipts, 43 repos); `results/round10/summary.json` (cumulative: 43 repos, 62 files); `INVENTORY_FINAL.md:3`, which supersedes the `INVENTORY.md` cited on 09-21 (130 in-scope files = 126 own current + 4 own pending-PR, 62 of them string-only); `results/round3_runtime.md` (7 bundles, 14 rows); `FOLD_REQUEST.md` (folded on the owner's GO, 2026-09-22).
[^r3pred]: Local: the audit's `results/round3_runtime.md`, "Predicted (static class) vs measured" (lines 68–78). On NIGHTLY 0.18.0.dev20260919: S-DROP OFF-PROMPT (Round 4 rerun), S-RAISE RAISE, both S-LEAK bundles ON-PROMPT, DUAL / PARTS / NONE ON-PROMPT. The 09-21 text said every string-only template drops the prompt or raises.
[^builder]: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/python/litert_lm_builder/litertlm_builder.py#L212-L236 (the enum) · https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/python/litert_lm_builder/litertlm_builder.py#L931 (override refused) · `litertlm_peek.py`, `litertlm_builder_cli.py` and `litertlm_core.py` at e2952685d have no tts/asr/acoustic/vocoder string.
[^loader]: 4cf0bf515: https://github.com/google-ai-edge/LiteRT-LM/commit/4cf0bf515d9fac84c001ba1c4afecceec08953e2 · https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/runtime/util/litert_lm_loader.cc#L70-L93 · section enum: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/schema/core/litertlm_header_schema.fbs#L105-L141
[^kokoro]: b5bc8faf8: https://github.com/google-ai-edge/LiteRT-LM/commit/b5bc8faf827c60757b9fd2e2af45879d3594ec88 · sections read: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/tts/kokoro/kokoro_factory.cc#L88-L145 and #L218-L234 of the same file · `-textnorm` / `-lexicon`: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/tts/kokoro/kokoro_factory.h#L36-L43 · `espeak-ng`: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/tts/kokoro/espeak_assets.h#L27 · voice names: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/tts/kokoro/common.h#L210-L213 · type detection: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/tts/tts_engine.cc#L64-L157
[^qwen3folder]: The factory compiles seven files from `model_folder` and reads no `.litertlm` resources: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/tts/qwen3_tts/qwen3_tts_factory.cc#L43-L95
[^ghrt]: [results/v0.17.0-generation.md](../results/v0.17.0-generation.md), lines 56–59 (LiteRT v2.2.0).
[^9482]: https://github.com/google-ai-edge/LiteRT/issues/9482#issuecomment-5401946253 · https://github.com/google-ai-edge/LiteRT/issues/9482#issuecomment-5764174764

Added in round 2 (2026-09-26):

[^capgrep]: This repo's `gh_assets_check.py:129` (xcframework zips) and `:170` (C API zip) count zip members matching `capabilit`. The v0.16.0 mac zip passed on `capabilities_c.h` (litertlm-convert `watch/on_release/channels/results_20260909/gh_assets.json`; that run's `Package.swift` pinned the v0.16.0 zips), the v0.17.0 mac zip lists `capabilities.h` (litertlm-convert `watch/on_release/results_20260909/tts_assets_check.log`), and `c/` on e2952685d has no file matching it: https://github.com/google-ai-edge/LiteRT-LM/tree/e2952685dd50c6ed24f067849a218d6249641c7b/c
[^relgatecopy]: Local: litertlm-convert `watch/on_release/run_on_release.sh:62-66` runs that repo's `watch/on_release/channels/run_channels.sh`. Against this repo on 09-26: `gh_assets_check.py` and `diff_tables.py` are identical; that `run_channels.sh` adds a `pypi` leg; `pins.env`, `channels_common.py`, `make_table.py` and the four shell runners differ, and neither copy is uniformly newer (its `maven_check.sh` was last committed 09-06, this repo's 09-01; its `run_channels.sh` 08-31). Its tables live in litertlm-convert `reports/`, where `release_artifact_gate_20260909_data/` holds the 56 checks of [results/v0.17.0-generation.md](../results/v0.17.0-generation.md), `pypi.json` included.
[^streaming]: 62839a736 added `class OmniStreamingSession` to `omni/omni_session.h` (https://github.com/google-ai-edge/LiteRT-LM/commit/62839a736917ee862402bff5ca3d5ca382c52c83); the diff of that header in f74763b54 removes the seven lines naming it (https://github.com/google-ai-edge/LiteRT-LM/commit/f74763b5420b61b83441eed103305d1cbdd72249), and the header at e2952685d has none: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/omni/omni_session.h
[^zero]: At e2952685d the only rules depending on `fake_llm_executor` (https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/runtime/executor/BUILD#L482-L505) and `fake_constraint` (https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/runtime/components/constrained_decoding/BUILD#L164-L175), across the 93 `BUILD` and `.bzl` files, are 13 `cc_test` rules. Local counts (`/usr/bin/grep -c` over `strings` output): the v0.16.0 `libCLiteRTLM_mac.dylib` (a universal binary from the cached `CLiteRTLM_mac.xcframework.zip`, sha256 `3ae6c876…`), read with `strings -` (every byte), gives 0 for `FakeLlmExecutor|FakeConstraint`, 0 for the engine and metadata patterns, 39 for `EmbeddingEngine` and 9,488 lines containing `6litert2lm`, so mangled names are visible; read with `strings -a` (sections only, the 09-09 script's form) the zeros stay and the last two drop to 2 and 376; the 0.17.1 AAR's `jni/arm64-v8a/liblitertlm_jni.so` (Gradle cache, AAR sha256 `a8aeaa6b…`) gives 0 for the engine, metadata and zero patterns and 5–6 for `EmbeddingEngine` in either mode; edge-llm-bench `ios/AsrBench/Frameworks/libasr_bench_static.a` (built 09-25 from `main` at 66058c82 over `//omni/asr:asr_engine`), read with `strings -a`, gives 0 for the zero control, 30 for `EmbeddingEngine` and 593 for `6litert4omni` (296 of them `6litert4omni3asr`, none `6litert4omni3tts`).
[^loaderproto]: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/runtime/util/litert_lm_loader.cc#L83-L91 (`TtsMetadata` / `AsrMetadata` `TfLiteModelType_Parse`, since 4cf0bf515) · `message Kokoro {}` and `message Qwen3Tts {}`: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/runtime/proto/tts_model_type.proto#L23-L26 · `Whisper`, `Parakeet`, `Moonshine`, `Qwen3Asr`: https://github.com/google-ai-edge/LiteRT-LM/blob/e2952685dd50c6ed24f067849a218d6249641c7b/runtime/proto/asr_model_type.proto#L23-L32 · local, on the same `libasr_bench_static.a` as the zero-control note, with `strings -a`: `Kokoro` 19, `Qwen3Tts` 19, `TtsMetadata` 48, `AsrMetadata` 46, `TtsEngine|TtsSession` 0, `omni/tts/` 0; the `Kokoro` lines are symbols such as `__ZN6litert2lm5proto25_Kokoro_default_instance_E`.
[^tsvmissing]: Local (09-26): `ls` of the nine `models.tsv` paths under `~/code/litertlm-convert` finds none; of their directories, `qwen35_work/out_ship_fp/` and `out/ministral-3-3b-reasoning-ext/` are gone, two are empty, and the rest keep JSON, logs, notes, metadata and scripts but no `.litertlm`; `find` and Spotlight turn up only `bitcpm-cann-1b_wi4b32_wi8.litertlm`, at `/Volumes/HD-SGDA/archive/litertlm-convert/bitcpm_work/`. `runtime_regate.sh:49-50` prints `SKIP` for a missing file and moves on.
[^bundle3577]: Local (09-26): litertlm-convert `minicpm52b_work/teamrecipe/out_int4hr/model.litertlm` links into the external archive disk, where the target is a link to itself (the archive's `_manifest/known_lost_links.txt:5`); `out_int4hr/model_fp32act.litertlm` (sha256 `036cfca8…`) is intact. Recipe: `teamrecipe/export_teamrecipe.sh:1-6` (the `models/minicpm5/README.md` conversion command, recipe as the first argument) and the bundle paragraph of the delegate issue draft, `clab/reply/issue_draft_delegate_v2_ja.md:9` (litert-torch 0.9.4, ai-edge-quantizer 0.9.0, litert-converter 0.4.0; about 2.5 min).
[^r3today]: Hub API (`/api/models/<repo>?blobs=true`, 09-26): heads 94e2ffb2 (qwen3_06b), 3ac79f5a (falcon_h1), 4ef1641b (lfm25_12b), 03d910d3 (qwen35_08b), bd9c98b7 (lfm25_230m), 3b7f1338 (granite42_3b), 0ca987c8 (sarashina22_05b); the first four LFS sha256 equal their `SHIPMENT.md` rows (lines 295, 197, 21, 44), the last three equal `results/round3/selection.json`. The 53 `SHIPMENT.md` rows are 35 S-LEAK, 12 S-RAISE and 6 S-DROP by the class in `results/round6/inventory_final.json`. Local, in the audit dir: `scripts/round3_download.py:17` and `:26-28` (deadline), `:43` and `:55-57` (selection row, pinned revision, size and sha asserts); `scripts/round3_run_pair.py:16` and `:28` (deadline), `:30` (venvs), `:45` (no log overwrite); `results/round3_runtime.md` (Qwen3 at `--max-num-tokens 512`: `FAILED_PRECONDITION: Chosen prefill work group size exceeds available state entries (512)`, rerun without the cap in round 4).
